language: python
python: "3.6"
# command to install dependencies
before_install:
  - sudo apt-get install -y raptor2
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - make test
